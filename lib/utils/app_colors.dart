import 'package:flutter/material.dart';

class AppColors {
  // --- CHILD PALETTE (From Reference Image) ---
  static const Color childBlue = Color(0xFF4FAAFD);   // Soft Sky Blue
  static const Color childPink = Color(0xFFF93C6F);   // Vibrant Pink
  static const Color childYellow = Color(0xFFFBA944); // Warm Gold
  static const Color childOrange = Color(0xFFFF8155); // Sunset Orange
  static const Color childGreen = Color(0xFF80FF9F);  // Fresh Mint Green
  static const Color childNavy = Color(0xFF3A455E);   // Deep Navy Text

  // --- ADMIN THEME (Ocean Blue & Cloudy Sky) ---
  static const Color oceanBlue = Color(0xFF2872A1);
  static const Color cloudySky = Color(0xFFCBDDE9);
  
  // --- PARENT THEME (Lemon Chiffon & Ultra Violet) ---
  static const Color lemonChiffon = Color(0xFFFEFACD);
  static const Color ultraViolet = Color(0xFF5F4A8B);

  // --- UI UTILITIES ---
  static const Color backgroundWhite = Color(0xFFF8F9FE);
  static const Color textDark = childNavy;
  static const Color lavender = Color(0xFFE8EAF6);
  static const Color primaryBlue = oceanBlue;
  static const Color teal = Color(0xFF00BFA5);
  static const Color accentOrange = Color(0xFFFF7043);
}