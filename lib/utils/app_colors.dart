import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (Indigo)
  static const Color primaryBlue = Color(0xFF3F51B5);
  // Highlight / Achievement color (Coral)
  static const Color accentOrange = Color(0xFFFF7043);
  // Progress / Success color (Teal)
  static const Color teal = Color(0xFF00BFA5);
  // Soft background / Accent color (Lavender)
  static const Color lavender = Color(0xFFE8EAF6);
  // Pure background
  static const Color backgroundWhite = Color(0xFFF8F9FE);
  // Text color
  static const Color textDark = Color(0xFF2D3436);
}