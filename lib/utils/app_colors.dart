import 'package:flutter/material.dart';

class AppColors {
  // --- ADMIN THEME (Ocean Blue & Cloudy Sky) ---
  static const Color oceanBlue = Color(0xFF2872A1);
  static const Color cloudySky = Color(0xFFCBDDE9);
  
  // --- PARENT THEME (Lemon Chiffon & Ultra Violet) ---
  static const Color lemonChiffon = Color(0xFFFEFACD);
  static const Color ultraViolet = Color(0xFF5F4A8B);

  // --- CHILD THEME (Rainbow/Pastel Palette from your image) ---
  static const Color childPink = Color(0xFFFF6B81);
  static const Color childOrange = Color(0xFFFFA07A);
  static const Color childYellow = Color(0xFFF9FF92);
  static const Color childGreen = Color(0xFF80FF9F);
  static const Color childBlue = Color(0xFF80B3FF);
  
  // --- UI ELEMENT & UTILITY COLORS ---
  static const Color lavender = Color(0xFFE8EAF6); 
  static const Color primaryBlue = oceanBlue;     // Primary Action Color
  static const Color accentOrange = Color(0xFFFF7043); // Secondary Action Color
  static const Color teal = Color(0xFF00BFA5);    // Success / Mastery Color
  static const Color backgroundWhite = Color(0xFFF8F9FE);
  static const Color textDark = Color(0xFF1E293B);
}